module.exports = function(app) {
  var controller = require("../controller/customerController");

    // app.route("/customers").get(controller.customers);
    // app.route('/customer/:id').put(controller.updateCustomer);
    // app.route('/customer/:id').delete(controller.deleteCustomer);
    // app.route('/customer').post(controller.inCustomer);
    // app.route('/customer/:id').get(controller.seaCustomer);
};
