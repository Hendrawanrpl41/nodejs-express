module.exports = function (app) {
    var account = require("../controller/accountController");

    // app.route("/accounts").get(account.selAccount);
    // app.route('/account/:id').put(account.updAccount);
    // app.route('/account/:id').delete(account.delAccount);
    // app.route('/account').post(account.insAccount);
    // app.route('/account/:id').get(account.seaAccount);
};
